describe("read", function() {

	it("should put info in dom", function() {
	String title;
	String body;
	String author;
	var likes;
	var dislikes;
	var dom = putInDom(title, body, author, likes, dislikes);
	expect(dom).toEqual($("#return").click(read.onReturnClick);
        $("#buttonLike").click(read.onLikeClick(title, body, author, likes, dislikes));
        $("#buttonDislike").click(read.onDislikeClick););
    
    });
    
    it("should highlight", function() {
    var high = highlight();
    expect(high).toEqual(
    $("#indexNav li").removeClass("active");
        $(which).parent().addClass("active"););
        
    });
});
    
    
	
    